<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Girls' Education — Empower Her</title>
  <style>
    /* Simple, responsive, accessible styles */
    :root{
      --bg:#f7fbff;
      --card:#ffffff;
      --accent:#2b6cb0;
      --text:#0f1724;
      --muted:#5b6b7a;
      --radius:12px;
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }
    body{
      margin:0;
      padding:24px;
      background:var(--bg);
      color:var(--text);
      line-height:1.5;
    }
    .container{
      max-width:900px;
      margin:0 auto;
    }
    header{
      display:flex;
      gap:16px;
      align-items:center;
      margin-bottom:20px;
    }
    .logo{
      background:var(--accent);
      color:white;
      width:64px;
      height:64px;
      border-radius:50%;
      display:flex;
      align-items:center;
      justify-content:center;
      font-weight:700;
      font-size:20px;
      box-shadow:0 6px 18px rgba(43,108,176,0.12);
    }
    h1{ margin:0; font-size:1.6rem; }
    p.lead{ color:var(--muted); margin:8px 0 0; }

    .grid{
      display:grid;
      grid-template-columns: 1fr 320px;
      gap:20px;
      margin-top:18px;
    }
    @media (max-width:820px){
      .grid{ grid-template-columns: 1fr; }
    }

    .card{
      background:var(--card);
      padding:18px;
      border-radius:var(--radius);
      box-shadow:0 6px 18px rgba(12,20,30,0.06);
    }

    .hero-img{
      width:100%;
      height:180px;
      border-radius:10px;
      background:linear-gradient(90deg, rgba(43,108,176,0.12), rgba(99,179,237,0.05));
      display:flex;
      align-items:center;
      justify-content:center;
      color:var(--accent);
      font-weight:600;
      font-size:1.05rem;
    }

    h2{ margin-top:0; color:var(--accent); font-size:1.15rem; }
    ul{ padding-left:1.1rem; margin-top:8px; color:var(--muted); }

    .cta{
      display:inline-block;
      margin-top:12px;
      padding:10px 14px;
      background:var(--accent);
      color:#fff;
      border-radius:10px;
      text-decoration:none;
      font-weight:600;
    }

    footer{
      margin-top:20px;
      text-align:center;
      color:var(--muted);
      font-size:0.9rem;
    }

    /* small info list */
    .info-list{ list-style:none; padding:0; margin:0; display:flex; gap:8px; flex-wrap:wrap; }
    .info-list li{
      background:#f1f6fb; color:var(--muted);
      padding:8px 10px; border-radius:8px; font-size:0.9rem;
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="logo" aria-hidden="true">GE</div>
      <div>
        <h1>Girls' Education — Empower Her</h1>
        <p class="lead">Simple actions &amp; awareness to help girls access quality education.</p>
      </div>
    </header>

    <div class="grid">
      <!-- main content -->
      <main class="card" role="main" aria-labelledby="about-heading">
        <div class="hero-img" role="img" aria-label="Illustration of girls learning">Education changes lives</div>

        <section id="about" aria-labelledby="about-heading" style="margin-top:14px;">
          <h2 id="about-heading">Why Girls' Education Matters</h2>
          <p style="color:var(--muted)">Education gives girls knowledge, confidence and the tools to shape their own futures. It reduces poverty, improves health, and benefits entire communities.</p>

          <h2 style="margin-top:12px;">Key Goals</h2>
          <ul>
            <li>Ensure safe access to school for every girl</li>
            <li>Provide scholarships and learning materials</li>
            <li>Promote gender-sensitive classrooms and teachers</li>
            <li>Support life skills and vocational training</li>
          </ul>

          <h2 style="margin-top:12px;">How You Can Help</h2>
          <ul>
            <li>Volunteer at local schools or tutoring programs</li>
            <li>Donate books, stationery, or funds</li>
            <li>Advocate for girls' rights in your community</li>
            <li>Mentor a student or run a career-awareness workshop</li>
          </ul>

          <a class="cta" href="mailto:info@example.org?subject=I%20want%20to%20help%20girls%27%20education">Get Involved — Email Us</a>
        </section>
      </main>

      <!-- sidebar -->
      <aside class="card" aria-labelledby="quick-heading">
        <h2 id="quick-heading">Quick Facts</h2>
        <ul class="info-li
