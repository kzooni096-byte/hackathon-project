<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Simple Website</title>
  <meta name="description" content="A simple single-page website template">
  <style>
    /* Simple, clean styling */
    :root{--accent:#2563eb;--bg:#f8fafc;--card:#ffffff}
    *{box-sizing:border-box}
    body{font-family:system-ui,-apple-system,Segoe UI,Roboto,'Helvetica Neue',Arial;line-height:1.5;margin:0;background:var(--bg);color:#0f172a}
    .container{max-width:980px;margin:24px auto;padding:16px}
    header{display:flex;align-items:center;justify-content:space-between;gap:12px}
    .logo{font-weight:700;color:var(--accent)}
    nav a{margin-left:12px;text-decoration:none;color:inherit}
    .hero{background:linear-gradient(90deg, rgba(37,99,235,0.08), rgba(99,102,241,0.03));padding:28px;border-radius:12px;margin-top:16px}
    .grid{display:grid;grid-template-columns:1fr;gap:16px;margin-top:16px}
    @media(min-width:800px){.grid{grid-template-columns:1fr 320px}}
    .card{background:var(--card);padding:16px;border-radius:10px;box-shadow:0 6px 18px rgba(2,6,23,0.06)}
    h1{margin:0 0 8px 0;font-size:1.6rem}
    p{margin:0 0 12px 0}
    footer{opacity:0.8;font-size:0.9rem;margin-top:20px;text-align:center}
    .btn{display:inline-block;padding:8px 14px;border-radius:8px;background:var(--accent);color:white;text-decoration:none}
    label{display:block;font-weight:600;margin-bottom:6px}
    input[type=text],input[type=email],textarea{width:100%;padding:8px;border:1px solid #e6edf3;border-radius:8px}
    textarea{min-height:100px}
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="logo">MySite</div>
      <nav>
        <a href="#home">Home</a>
        <a href="#about">About</a>
        <a href="#contact">Contact</a>
      </nav>
    </header>

    <section id="home" class="hero card">
      <h1>Welcome to this simple website</h1>
      <p>This is a minimal, single-file HTML template you can edit and expand. It includes a header, responsive layout, and a contact form.</p>
      <a class="btn" href="#contact">Get in touch</a>
    </section>

    <main class="grid">
      <div class="card">
        <h2 id="about">About</h2>
        <p>This block is for the main content. Replace this text with your own content: information about a project, a small portfolio, a blog intro, or anything else.</p>
        <h3>Features</h3>
        <ul>
          <li>Responsive layout (works on mobile and desktop)</li>
          <li>Simple styling with CSS only</li>
          <li>Copy-paste and start editing</li>
        </ul>
      </div>

      <aside class="card">
        <h3>Contact</h3>
        <form id="contact" onsubmit="handleSubmit(event)">
          <label for="name">Name</label>
          <input id="name" name="name" type="text" placeholder="Your name" required>

          <label for="email">Email</label>
          <input id="email" name="email" type="email" placeholder="you@example.com" required>

          <label for="message">Message</label>
          <textarea id="message" name="message" placeholder="Write a short message" required></textarea>

          <div style="margin-top:10px">
            <button type="submit" class="btn">Send (demo)</button>
          </div>
        </form>
        <p style="margin-top:10px;font-size:0.9rem;opacity:0.9">Note: this template doesn't send messages — you'll need a backend or service (Formspree, Netlify Forms, etc.) to receive submissions.</p>
      </aside>
    </main>

    <footer>
      © <span id="year"></span> MySite — Built with a single HTML file
    </footer>
  </div>

  <script>
    // Small JS to handle the demo contact form and set the year
    document.getElementById('year').textContent = new Date().getFullYear();

    function handleSubmit(e){
      e.preventDefault();
      const name = document.getElementById('name').value;
      alert('Thanks, ' + name + '! This is a demo — no message was sent.');
      e.target.reset();
    }
  </script>
</body>
</html>
